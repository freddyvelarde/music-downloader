const mainUrl = "http://192.168.0.9:7676";

export const start_download_url = `${mainUrl}/search`;

export const download_url = `${mainUrl}/download`;

export const get_all_songs = `${mainUrl}/songs`;

export const rename_song = `${mainUrl}/rename`;
